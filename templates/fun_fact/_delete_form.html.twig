<form method="post" action="{{ path('app_fun_fact_delete', {'id': fun_fact.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fun_fact.id) }}">
    <button class="btn">Delete</button>
</form>
